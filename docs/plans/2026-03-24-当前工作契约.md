# 当前工作契约

## 目的
这份文件只服务当前设计执行，避免长对话压缩后重复回放背景。

## 不可回退的共识
- 产品名：`彩盒`
- 核心体验：先发现，再带回，再让颜色待一会儿，再收成一页
- 不伪造拥有感
- 系统越来越隐身，发现越来越显眼
- 第一平台：iPhone
- 设计主现场：`prototype/` 下的本地 Web 原型
- 不再使用 `Calicat` / `Pencil` 作为主设计现场
- 不再用 sub-agent 做当前主线设计

## 已封板的结构
- 主路径：
  - `Home -> See / Live -> See / Frozen -> 显色 -> 收进彩盒 -> 彩盒 / 新收页打开态 -> 我的彩盒 / 页列表 -> 单页回看`
- `See` 当前收为：
  - `Live`
  - `Frozen`
- 原 `Picked`：
  - 不再作为独立节点保留
  - 并入 `Frozen / 已选态`
- `收进彩盒` 默认进入：
  - `新收页打开态`
- `我的彩盒` 的最小结构：
  - `页列表`
  - `单页回看`
  - 从单页轻回到新的观察

## 当前不做的事
- 不再讨论首页的大方向
- 不再写给工具的任务书
- 不再整理大量中间过程文档
- 不再重新辩论产品原则
- 不再回到 Pencil / Calicat 画布主线

## 当前唯一任务
重做并统一以下关键页：
- `Frozen`
- `显色`
- `新收页打开态`
- `我的彩盒 / 页列表`
- `我的彩盒 / 单页回看`

目标：
- 去掉重复表达
- 拉开页面职责
- 保持 Apple 风格约束下的液态玻璃控制层
- 内容层更像“页”，不是卡片堆砌
- 不靠长文案解释状态

其中当前优先级最高的是：

- 先完成 `Frozen -> 显色` 的新链路定义与高保真原型
- `显色` 当前作为工作名使用，不等于最终界面命名

## 首页当前状态
- 首页只作为基线，不是当前主战场
- 允许后续统一 token 时顺手微调
- 不允许再次把主要精力拉回首页

## 当前代码入口
- HTML：`/Users/yuxihe/repo/arrwuu/kColors/prototype/index.html`
- CSS：`/Users/yuxihe/repo/arrwuu/kColors/prototype/styles.css`
- JS：`/Users/yuxihe/repo/arrwuu/kColors/prototype/app.js`

## 执行规则
- 每次上下文压缩后，先回读本文件
- 只汇报实际改动，不重复宣誓要做什么
- 如果任务改变，先改这份文件，再改页面

## 当前设计锚点
- 当前关于 `Frozen / 显色 / AI 弱建议 / 重入素材` 的最新讨论锚点：
  - `/Users/yuxihe/repo/arrwuu/kColors/docs/plans/2026-03-24-显色命题草案.md`

## 2026-03-24 晚间进展

今天的有效进展主要有三点：

- `Try` 已正式脱离原有心智，当前工作名改为 `显色`
- `Picked` 已确认并入 `Frozen / 已选态`
- `Frozen` 已从“全屏图 + 覆盖面板”的旧思路中剥离，开始明确为：
  - 上部：观察卡面
  - 下部：片段承接盘

当前高保真原型阶段性状态：

- `Home`
  - 只保留为基线，不再作为当前主战场
- `Live`
  - 继续沿用旧版方向，当前无需大改
- `Frozen`
  - 已完成从覆盖式叠层到独立双区结构的方向切换
  - 当前版本已基本可表达概念，但仍需继续收空间效率、顶部区和承接盘细节
- `显色`
  - 结构方向已建立
  - 主舞台还未真正成立，仍是明天的重点

## 今天的教训

- 不能再拿旧版 `Live/Frozen` 的覆盖式玻璃层心智来修新 `Frozen`
- 一旦 `Frozen` 已被定义为独立页面，就必须用独立页面的方式处理：
  - 不再用承接层压住图片
  - 不再用拖图补救布局
  - 不再靠长说明文案填状态
- 高保真原型里，`空态 -> 1 个片段 -> 多个片段` 的结构高度必须尽量稳定
- AI 建议只能是轻提示，不能稳定吃掉一整块承接区

## 明天的起手点

只继续推进两页：

- `Frozen`
  - 继续压缩承接盘空耗
  - 继续弱化 AI 建议的占位感
  - 统一顶部区、观察卡面、承接盘三者的节奏
- `显色`
  - 正式重做主舞台
  - 优先解决“来源为底、片段站位为主”仍未成立的问题
